# Direct dependencies only. Run `pip install -r requirements.txt`; pip resolves the rest.
# For a full lockfile: pip freeze > requirements.lock.txt (or use pip-tools / uv lock).

fastapi[standard]==0.135.3
SQLAlchemy==2.0.37
alembic==1.18.4
psycopg[binary]==3.3.3
pydantic-settings==2.13.1
python-dotenv==1.2.2
httpx==0.28.1
PyJWT==2.10.1
bcrypt==4.3.0
